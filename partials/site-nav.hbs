<nav class="site-nav">
    <div class="site-nav-left">
      {{^is "home"}}
        <a href="{{@blog.url}}">
            {{#if @blog.icon}}
                <img src="{{@blog.icon}}" alt="{{@blog.title}} icon" />
            {{/if}}
        </a>
      {{/is}}
    </div>
    <div class="site-nav-right">
        {{#if @blog.navigation}}
            {{navigation}}
        {{/if}}
    </div>
</nav>
